# dev namespace — local Docker only today
TEMPORAL_NAMESPACE=dev
TEMPORAL_ADDRESS=localhost:17233

# TLS / Temporal Cloud fields left blank locally; see prod.env.example for the
# seam these fill in once Cloud is purchased.
TEMPORAL_TLS_CERT_PATH=
TEMPORAL_TLS_KEY_PATH=
TEMPORAL_CLOUD_API_KEY=
